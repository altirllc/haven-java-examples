apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yaml
- service-api.yaml
- service-mcp.yaml
- service-ui.yaml
- ingressroute.yaml

labels:
- includeSelectors: true
  pairs:
    haven.layer: business
    haven.service: loom
